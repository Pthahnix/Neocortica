# ── Backend (Railway) ──────────────────────────────────────────────

# API Key for LLM
API_KEY_MODEL = 'your_llm_api_key'

# Base URLs
BASE_URL_MODEL = 'your_llm_base_url'
BASE_URL_ARXIV2MD = 'https://arxiv2md.org/api/ingest'
BASE_URL_ARXIV = 'https://export.arxiv.org/api/query'

# LLM Model
MODEL_NAME = 'your_llm_model'

# Paper cache directory (use Railway Volume mount path in production)
PAPER_CACHE = '.paper'

# Optional: API key for authenticating MCP client requests
NEOCORTICA_API_KEY = ''

# ── MCP Client (Local) ────────────────────────────────────────────

# Backend API URL (Railway public domain)
NEOCORTICA_API_URL = 'https://your-app.up.railway.app'
