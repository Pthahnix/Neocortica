{
  "name": "neocortica-mcp",
  "version": "2.0.0",
  "description": "NEOCORTICA MCP Client — HTTP forwarding layer",
  "type": "module",
  "scripts": {
    "start": "tsx src/mcp_server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1",
    "dotenv": "^16",
    "zod": "^3"
  },
  "devDependencies": {
    "tsx": "^4",
    "@types/node": "^22"
  }
}
